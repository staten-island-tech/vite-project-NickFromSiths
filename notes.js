import { techProducts } from "./app/JS/Products";

function showByGenre(x) {
  //   document.querySelector(".container").innerHTML = "";
  const tech = tech.Array.from();
  const y = techProducts.filter((i) => i[x]);
  console.log(y);
}

showByGenre(features);
